# Cistern UX — Round-2 Qualities (legibility + interaction lenses)

> Collector: ux-collect-r2, 2026-09-07. Fresh pass over the post-TOP-30 surface.
> Evidence: batch captures `playtest/R2-0*.txt` via `playtest/r2.el`
> (`emacs -Q --batch -l src/cistern.el -l playtest/r2.el -f cistern-r2-run`).
> Excludes everything TOP-30 already shipped (Q01–Q30: strip segments, dead-pipe
> glyph existence, persisted log severity, death panel existence, armed badge
> existence, refusal hints, `L` log, regret-window mechanics, auto-run badge,
> starter card). Ranked by player impact.

| # | Quality | State | Evidence | Refactor idea |
|---|---------|-------|----------|---------------|
| R2-1 | The armed badge must fit the 95-col strip it was reserved for | **VIOLATES** — header renders 126 cols with `ARMED: PIPE — CLICK PLACES, ESC CANCELS` appended after `REP`; the tail wraps onto a phantom second line and shifts every buffer row below it (breaking `cistern-view--header-lines`=3 geometry for `cistern-view--cell-at`) | `R2-02-armed-badge.txt`; cold header is 85 cols, armed is 126 (`cistern-view.el:190-197`, `cistern--copy` `badge-armed` :802) | Move the badge to the reserved dim slot **below** the strip (own line), or shorten copy to `ARMED: PIPE (ESC)` ≤ 10 cols. |
| R2-2 | Death messaging speaks once, not four times, and stays ≤95 cols | **VIOLATES** — on condemnation the header grows to 130 cols (`!! SECTOR CONDEMNED — CONTAMINATION LIMIT` pushed past 95), and the same fact appears on the header `!!` segment, the banner panel, the pressure line, and a log line — four rows of one screen | `R2-06-condemned-panel.txt` L1/L20/L22/L25; `cistern-view.el:194-196`, `:335`, `:461-468` | Keep the Q23 panel as the single death surface; header `!!` shortens to `!! CONDEMNED`; pressure line goes empty while `over`. |
| R2-3 | Bottom UI lines respect the 95-col budget | **VIOLATES** — help line is 168 cols (wraps, orphaning `t/p/K arm — click to place` onto an unpropertized-looking second row) and the legend is 102 (orphaning `α worker`) | `R2-01-cold-start.txt` line widths 168/102; `cistern-view.el:220-226`, `:228-242` | Split the help line into two deliberate dim rows (cursor/actions, then arm+meta), and let the legend wrap on a `GLYPHS:`-aligned continuation. |
| R2-4 | Every shipped interaction is named on a player-visible surface (`?` briefing or help line) | **VIOLATES** — `L` full log, `ESC`/`u` disarm, and `C-u r` slow auto-run appear in **no** player-facing copy; the only ESC mention is the armed badge, i.e. you must already be armed to learn you can cancel | `R2-07-help.txt` (mentions-L=nil, ESC=nil, slow=nil); `cistern.el:60,58-59`, `cistern.el:187` | Three lines in the briefing CONTROLS: `L full log`, `ESC/u cancel armed verb`, `C-u r slow auto-run (1 tps)`. |
| R2-5 | The free same-tick regret window is discoverable at the moment it matters | **VIOLATES** — Q30 mechanics shipped but nothing teaches "demolish in the build tick is free"; the player learns the 3-alloy price from the help line and never learns the free-undo case | `R2-07-help.txt` mentions-regret=nil; only source comments mention it (`cistern-game.el:88-94`) | Demolish inspector on a cell built this tick appends `— SAME-TICK: FREE UNDO`; or the build hint on placement says `CLICK d TO UNDO FREE`. |
| R2-6 | The strip's GOALS/SCORE/REP segments are explained somewhere | **VIOLATES** — the briefing has no GOALS/goal-card mention at all; a new player sees `GOALS 0/2` on tick 0 with zero idea what a "goal" is or that cards exist (Q03 made the loop live; nothing names it) | `R2-07-help.txt` mentions-goals=nil; briefing THE LOOP (`cistern.el:217-229`) | One briefing paragraph: `GOALS n/m tracks the active goal card; complete it for score and trophies.` |
| R2-7 | Dead pipe inspects as dead | **VIOLATES** (partially — glyph/faces/legend are Q12 and **meet**: ╌ renders distinct from floor, `cistern-pipe-dead` grey50, legend generated) — but the cursor inspector on a severed pipe gives the generic `PIPE — the only wire; keep it short`, never "dead/severed"; the map shows the problem, the inspector doesn't | `cistern-view.el:244-274` (pipe falls to `:274` default; no severed branch) | Add a pipe branch: severed → `PIPE — DEAD: NOT CONNECTED — REWIRE (p)`. |
| R2-8 | Tutorial line yields to real pressure | **VIOLATES** — the cyan-bold `TUTORIAL 1/3 …` line sits directly under the pressure line; when pressure is red-bold CRITICAL both high-salience lines stack, and the tutorial keeps coaching after `SECTOR CONDEMNED` | `R2-06-condemned-panel.txt` L22 (red) vs L23 (cyan bold); `cistern-view.el:484-486` | Face the tutorial line `cistern-dim` whenever pressure is not NOMINAL, and suppress it while `over`. |
| R2-9 | Worker naming agrees across surfaces | **VIOLATES** — the breach log still says `BREACH — CREATOR δ OVERFLOWED…` while map, inspector, and legend all say `worker`; two words for the same entity (Q14 unified the *glyph*, not the *noun*) | `R2` run 05: `("BREACH — CREATOR δ OVERFLOWED AT (6,13)" . error)`; legend `α worker` (`cistern-view.el:242`) | Change the breach format string to `BREACH — WORKER δ …` (or drop the noun: `BREACH — δ OVERFLOWED`). |
| R2-10 | The `L` log buffer exits like everything else | **VIOLATES** (minor) — the full-log buffer is fundamental-mode, read-only: `q` does nothing, no `press q` hint; the game window is underneath and the player is stranded with arrows only | `cistern.el:191-203` (plain `pop-to-buffer`, no mode); `R2` run 08 confirmed read-only, 2+ lines | One line at the buffer top `— press q to close —` + `(use-local-map special-mode-map)` / `quit-window` keybinding. |

## Meets (fresh-verified, keep as-is)

- **Log severity persistence (Q13)**: breach entry at tick 50 still renders `cistern-toilet-down` (red) in the tail 3 ticks later — `R2` run 05.
- **Refusal hints at the cursor (Q17/Q18)**: armed wall-click renders `NO FLOOR THERE — AIM FOR OPEN FLOOR` directly under `CURSOR (2,0): MEGASTRUCTURE WALL` for exactly one tick, then vanishes; no layout shift — `R2-04-refusal-hint.txt` L21-22.
- **Arm-then-click teaching while armed (Q19)**: badge text is exactly the right contract (`CLICK PLACES, ESC CANCELS`), armed state never blocks input, at-cursor refusal refuses cleanly without arming (`cistern.el:136-147`).
- **Dead-pipe glyph distinctness (Q12)**: ╌ ≠ floor `·`, grey50 face vs grey40 floor, legend auto-generated, no double listing — `R2-03-dead-pipe.txt`, face probe `(9 (cistern-cursor cistern-pipe-dead))`.
- **`L` full-log mechanics (Q16)**: uncapped, oldest-first, read-only — `R2` run 08.
- **Starter card renders from tick 0 (Q03/Q04)**: cold start shows `GOALS 0/2` (visibility part meets; comprehension is R2-6).
